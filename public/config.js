/**
 * 前台配置（**不含任何账号凭据**）
 *
 * 历史原因：原站的账号与口令是直接写在本文件里的（明文），由前端自行比对。
 * 现在账号数据统一由后端管理（data/db.json，后台可增删改查），
 * 登录改走 POST /api/auth/login，本文件不再保存任何口令或个人资料 ——
 * 否则仓库一旦公开，这些内容就会直接暴露给全网。
 *
 * 保留该全局对象是为了兼容旧页面的引用（例如读写 window.WEB003_CONFIG.apiBaseUrl）。
 */
window.WEB003_CONFIG = {
  // 不再存放账号：登录由后端校验，需要账号请到后台界面维护。
  accounts: {},
  // 前后端同源部署时留空即可；分域部署时填后端地址，例如 http://localhost:3000
  apiBaseUrl: '',
};
